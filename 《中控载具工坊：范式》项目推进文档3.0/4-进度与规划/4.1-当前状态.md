# 当前状态

> 更新于 2026-06-11（国际化全量覆盖 + 开源准备 + 武器系统终版）

---

## ✅ 已完成功能

| # | 功能 | 完成于 | 实现记录 |
|---|------|--------|---------|
| 1 | 核心标记方块 (SeatBlock) | 05-29 | `2-功能清单/2.2-3C控制系统.md` |
| 2 | 上车/下车系统 | 05-29 | `2-功能清单/2.2-3C控制系统.md` |
| 3 | 轨道摄像机 (Plan B→B1) | 05-30 | `2-功能清单/2.2-3C控制系统.md` |
| 4 | 通用驾驶舱双方块结构 | 05-31 | `2-功能清单/2.2-3C控制系统.md` |
| 5 | 唯一检测 + 独占管理 | 05-31 | `2-功能清单/2.2-3C控制系统.md` |
| 6 | 宽泛射线命中 | 05-31 | `2-功能清单/2.2-3C控制系统.md` |
| 7 | 键盘输入读取（技术验证） | 05-31 | `2-功能清单/2.2-3C控制系统.md` |
| 8 | SubLevel Chunk 扫描 | 06-01 | `3-架构与文件结构/3.1-SubLevel物理架构.md` |
| 9 | 悬挂测试方块（视觉+力学） | 06-01 | `2-功能清单/2.3-悬挂与载具控制.md` |
| 10 | 摩擦圆物理模型 | 06-01 | `2-功能清单/2.3-悬挂与载具控制.md` |
| 11 | 下车全场景安全覆盖 | 06-01 | `2-功能清单/2.2-3C控制系统.md` |
| 12 | 载具按键控制系统（C 键 GUI） | 06-01 | `2-功能清单/2.3-悬挂与载具控制.md` |
| 13 | 手刹物理模型 v3（最终方案） | 06-03 | `2-功能清单/2.3-悬挂与载具控制.md` |
| 14 | 座舱动力系统（发动机+变速箱+差速） | 06-04 | `2-功能清单/2.4-座舱动力系统.md` |
| 15 | Q/E 换挡 | 06-04 | `2-功能清单/2.4-座舱动力系统.md` |
| 16 | 车辆调试覆盖层 | 06-04 | `2-功能清单/2.5-调试与监控.md` |
| 17 | 质量同步 (vehicleMass) | 06-04 | `2-功能清单/2.5-调试与监控.md` |
| 18 | 自适应摄像机（高度+距离） | 06-05 | `2-功能清单/2.2-3C控制系统.md` §自适应摄像机 |
| 19 | 玩家完全隐藏（模型+粒子） | 06-05 | `2-功能清单/2.2-3C控制系统.md` §上车效果 |
| 20 | **质量自适应扭矩** | **06-06** | `2-功能清单/2.4-座舱动力系统.md` §1.2 |
| 21 | **连续油门系统** | **06-06** | `2-功能清单/2.4-座舱动力系统.md` §1.3 |
| 22 | **扭矩平衡负载模型** | **06-06** | `2-功能清单/2.4-座舱动力系统.md` §1.4 |
| 23 | **动态载荷转移** | **06-07** | `2-功能清单/2.3-悬挂与载具控制.md` §5 |
| 24 | **Brush 轮胎侧偏模型** | **06-07** | `2-功能清单/2.3-悬挂与载具控制.md` §6 |
| 25 | **轮胎物理参数系统**（胎压/胎宽/爆胎） | **06-09 精简** | `2-功能清单/2.3-悬挂与载具控制.md` §运行时参数 |
| 26 | **炮塔底座方块** (TurretBase) | **06-07** | 右键生成物理化砂轮+避雷针 SubLevel |
| 27 | **锚点 A 坐标系统** (AnchorPoint) | **06-07** | X/Y/Z 可配置坐标，NBT 持久化 + S2C 包推送 |
| 28 | **三色轴线渲染** (AxisLines) | **06-07** | 每 SubLevel 100 格三色轴线 + 每地毯 3 格三色焦点十字线（RenderType.LINES，Sable debug render 模式） |
| 29 | **旋转轴承** (RotaryConstraint) | **06-07** | 砂轮↔载具 RotaryConstraint 铰链连接，anchor 对齐地毯定位点，局部 Y 轴旋转，碰撞禁用 |
| 30 | **侧向载荷转移符号修复** | **06-08** | 从 `-accelX` 修正为 `+accelX`，右转右轮正确减载 |
| 31 | **扭矩曲线** | **06-08** | RPM 依赖的扭矩修正，中段峰值 ~3,400 RPM |
| 32 | **发动机-轮速耦合** | **06-08** | engineRPM = max(wheelRPM × gearRatio × 主减速比, idle)，消除解耦感 |
| 33 | **换挡 RPM 同步** | **06-08** | 换挡瞬间 engineRPM ×= newRatio/oldRatio |
| 34 | **指令转速分离** | **06-08** | P 控制器目标用指令转速而非被耦合的实际转速 |
| 35 | **主减速比 FINAL_DRIVE_RATIO** | **06-08** | 14.0，替代改齿比压极速的思路 |
| 36 | **loadFactor 分母修正** | **06-08** | totalMaxForce /= count，不再低估 4× |
| 37 | **扭矩曲线平坦化** | **06-08** | TORQUE_IDLE_FRACTION 0.35→0.80 |
| 38 | **MIN_IMPULSE_MULTIPLIER 合理化** | **06-08** | 500→30，摩擦预算从 45g 降到 2.75g |
| 39 | **CORNERING_STIFFNESS 补偿** | **06-08** | 10→20，补偿 MIN_IMPULSE 降低后的回正刚度 |
| 40 | **二次方速度阻尼 DRAG_COEFFICIENT** | **06-08** | 0.0045，自然限制极速到 ~120 km/h |
| 41 | **滚动阻力清理** | **06-08** | 移除 `/0.4` 向后兼容残骸，crrBase 0.015→0.035 |
| 42 | **轮胎参数系统精简** | **06-09** | 6个调试残留运行时参数→编译时常量，仅保留胎压为玩家可调；TireConfigC2SPacket从7参数简化为仅(BlockPos, double) |
| 43 | **炮塔俯仰 (GenericConstraint ANGULAR_X解锁)** | **06-09** | 避雷针↔砂轮从全锁定改为解锁ANGULAR_X，实现高低机俯仰；旋转中心在约束锚点中点 |
| 44 | **约束锚点偏移硬编码** | **06-09** | 12个ANCHOR_*常量化（rod/grindstone_rod/grindstone_swivel/vehicle_carpet × XYZ）；Config.java turret.constraint_anchors节保留供未来继续使用 |
| 45 | **多SubLevel约束链技术文档** | **06-09** | 5.1-关键技术要点.md 新增§二十六，含4条血泪教训+完整成功配方+配置参考+检查清单 |
| 46 | **基本武器系统（射线检测 + B 键 + 准星）** | **06-10** | WeaponOverlay.java：1000格射线检测（方块/实体/SubLevel AABB）+ 2×2白色准星 + B键触发 |
| 47 | **持续自动瞄准（每 tick 自动更新）** | **06-10** | ClientEvents.onClientTick() 取消冷却，上车后每 tick 执行射线检测 |
| 48 | **AABB 射线检测过滤自身载具** | **06-10** | WeaponOverlay.performRaycast() 跳过当前挂载 SubLevel，+ 2格最小距离过滤 |
| 49 | **TurretTargetC2SPacket 网络包** | **06-10** | C→S 目标坐标，服务端扫描载具内 TurretBaseBlockEntity 并调用 TurretAimController.setTarget() |
| 50 | **方向机/高低机独立控制** | **06-10** | 方向机=砂轮Y轴旋转（RotaryConstraint），高低机=避雷针ANGULAR_X俯仰（GenericConstraint） |
| 51 | **PlayerMountTracker tick() 驱动** | **06-10** | 每服务端 tick 调用 TurretAimController.tick(level) |
| 52 | **TurretBaseBlock 改为 KineticBlock + ICogWheel** | **06-10** | 继承 Create 齿轮系统，block 提供 RPM 输入接口（用于应力网络诊断） |
| 53 | **TurretBaseBlockEntity 改为 KineticBlockEntity** | **06-10** | 继承 Create 动能方块，使 setMotor 每 tick 正常响应（参考 SwivelBearing） |
| 54 | **调试齿轮方块** (DebugGearBlock) | **06-10** | 小型齿轮方块，N 键切换调试输出，每 tick 打印 RPM 状态用于诊断 Create 应力网络 |
| 55 | **Create RPM 阶跃特性诊断** | **06-10** | 通过 DebugGear 实测证明 Create 应力网络转速变化是**瞬时的**（0→±16，±16→±88，±88→0 均在 1 tick 内完成，无渐变过程） |
| 56 | **位置模式 PD 伺服（最终方案）** | **06-10** | `updateYawServo()` 每 tick position-mode setMotor（kP=200/kD=16），参考 SwivelBearing 架构 |
| 57 | **绝对角度定位** | **06-10** | `setTargetYawAbsolute(degrees)` 替代增量调节，AimController 直接算出绝对目标角度 |
| 58 | **atan2↔约束坐标镜像修正** | **06-10** | `targetYawRad = -(currentYawRad + yawErr)`，atan2 逆时针为正，约束顺时针为正 |
| 59 | **瞄空气持续火控** | **06-10** | 射线无命中时返回末端点（类型 "Air"），炮塔持续指向鼠标方向（无限远目标） |
| 60 | **可配置偏航校准偏移** | **06-10** | Config.java 新增 `TURRET_YAW_OFFSET`（范围 -180~180°，默认 0°），补偿瞄准偏差 |
| 61 | **车体局部坐标系稳定瞄准** | **06-11** | 将目标方向变换到车体局部空间计算偏航（`qVehInv.transform(dirWorld)`），不受俯仰/侧倾/翻转影响 |
| 62 | **高低机位置模式 PD 伺服** | **06-11** | 与方向机统一为 position-mode setMotor，kP=6000/kD=400 对抗悬臂重力下垂 |
| 63 | **炮口-摄像机瞄准/伤害分离** | **06-11** | 瞄准用摄像机射线（TurretTargetC2SPacket），伤害用炮口射线（WeaponFireC2SPacket） |
| 64 | **开火系统** | **06-11** | WeaponC2SPacket 命中坐标 → 服务端 2 点伤害 + 暴击粒子 + 雪球/鸡蛋音效 |
| 65 | **多炮台独立开火** | **06-11** | fireAllTurrets() 遍历载具内所有 TurretBaseBlockEntity，每炮台独立计算炮口+发包 |
| 66 | **方块面中心瞄准（已替换）** | **06-11** | ~~getBlockFaceCenter() 面几何中心~~ → 改为 blockHit.getLocation() 精确表面交点（#73） |
| 67 | **按住连发** | **06-11** | isDown() + 3 tick 最小间隔（mc.level.getGameTime() 计时） |
| 68 | **无视无敌帧** | **06-11** | entity.invulnerableTime = 0 清除受伤免疫，每发必中 |
| 69 | **弹道渲染性能优化** | **06-11** | 开火时缓存炮口+命中位置，渲染器直接读，零 SubLevel 扫描 |
| 70 | **调试轴线默认隐藏** | **06-11** | 仅 F3+B（显示碰撞箱）时显示 |
| 71 | **发射键改为鼠标左键** | **06-11** | 移除 B 键 + 移除自动发射 + 鼠标左键按住连发 |
| 72 | **弹道特效持续时间** | **06-11** | fireTrailTicks = 2 tick（约 0.1 秒一闪而过） |
| 73 | **瞄准点精确命中坐标** | **06-11** | 移除 getBlockFaceCenter()，方块命中直接使用 blockHit.getLocation() 精确表面交点 |
| 74 | **实体命中点 AABB 精确计算** | **06-11** | 手动 AABB.clip() 替代 entityHit.getLocation()，避免返回脚部位置 |
| 75 | **射线穿透名单系统** | **06-11** | ClipContext.OUTLINE→COLLIDER（自然忽略无碰撞箱方块）+ 树叶穿透循环 + isPenetrable() |
| 76 | **VehicleKeyConfigScreen 国际化** | **06-11** | 所有硬编码中文标签 → Component.translatable()，KEY_LABEL_KEYS 数组统一管理 |
| 77 | **GrindstoneConfigScreen 国际化** | **06-11** | 方向按钮/关闭/锚点标签/保存按钮全部 translatable |
| 78 | **调试覆盖层国际化** | **06-11** | VehicleDebugOverlay 8 个标签改为 Component.translatable() |
| 79 | **消息/提示文本国际化** | **06-11** | ClientEvents 开火/射线/调试信息 + TurretTargetC2SPacket 服务端消息全部 translatable |
| 80 | **Config.java 双语注释** | **06-11** | 所有 .comment() 改为英中双语格式 |
| 81 | **完整中英文语言文件** | **06-11** | en_us.json + zh_cn.json 各约 100 条目，覆盖方块/物品/按键/消息/屏幕/覆盖层/配置全层级 |
| 82 | **配置翻译键全层级修复** | **06-11** | config.iac_p.* → iac_p.configuration.*，补齐三级菜单所有纯键名/button/tooltip |
| 83 | **开源协议与社区规范** | **06-11** | LICENSE (LGPL-3.0) + CONTRIBUTING.md + CODE_OF_CONDUCT.md + SECURITY.md |
| 84 | **GitHub 工单/PR 模板** | **06-11** | ISSUE_TEMPLATE/bug_report.md + feature_request.md + config.yml + PULL_REQUEST_TEMPLATE.md |
| 85 | **英文技术文档** | **06-11** | docs/en/ 7 篇文档：00-Quick-Start / 01-Getting-Started / 02-Feature-Overview / 03-SubLevel-Physics / 04-Code-Map / 05-Troubleshooting / 06-Current-Status |
| 86 | **CI 工作流增强** | **06-11** | build.yml 添加 Gradle 缓存 + 构建制品上传到 GitHub Actions Artifacts |
| 87 | **安全审计** | **06-11** | 全量扫描 API 密钥/Token/路径泄露，修复 `1.2-上手引导与编译.md` 中的本地路径泄露 |

## 🐛 已修复 Bug

| 问题 | 修复日 | 根因 | 修复方案 |
|------|--------|------|---------|
| 手刹 18g 钉地 | 06-03 | `brakeMag` 误用含 51 倍膨胀的 `frictionBasis` | v3：`springImpulse` 真实法向力基数，≈0.35g |
| 松 W 后 RPM 不降 | 06-04 | `putIfAbsent(false)` 不覆盖 + `enqueueWork` 时序晚于 BE tick | 废弃共享 Map，改用 CockpitBE 直接扫描 |
| CockpitBE.tick() 不执行 | 06-04 | `EntityBlock` 无 ticker | 改为 `IBE<CockpitBlockEntity>` |
| RPM 覆盖层不更新 | 06-04 | 同根因——tick() 不执行 | IBE 修复后双端 tick() 激活 |
| 极速超音速 (Mach 1.6) | 06-04 | 扭矩 80 Nm 过高 + 5 档超比档 0.7 | 扭矩 80→30，齿比 0.7→1.0 |
| 质量显示不精确 | 06-04 | `getInverseNormalMass()` ≠ 总质量 | 改用 `MassData.getMass()` |
| 摄像机极点偏航角突变 | 06-05 | 俯仰 ±90° 时 `horizontalDist=0` → `atan2(0,0)=0` → 偏航突变为 -90° | 水平距离 < 1e-4 时沿用 entity.getYRot() |
| 玩家模型随摄像机旋转 | 06-05 | SubLevel 全四元数旋转（含 yaw）与 yBodyRot 双重叠加 | 改为完全隐藏玩家模型，放弃旋转对齐 |
| **转向时车减速抖动** | **06-07** | **Brush 模型中 `tan(α)` 在大侧偏角时发散，导致侧向力震荡；纵向速度阈值 0.5 过低** | **侧偏角钳制 ±45°，阈值改为 totalSpeed > 1.0，添加 1.0~2.0 平滑混合** |
| **原地停放自旋** | **06-07** | **`forwardSpeedAbs > 0.5` 阈值过低，微小扰动就触发 Brush 模型** | **改为 `totalSpeed > 1.0`，低速纯阻尼** |
| **摩擦需求比显示不准确** | **06-07** | **显示分母用 frictionBudget（500×膨胀），永远偏低（如 17%）** | **改用自然摩擦预算 `max(adjustedSpringImpulse, nm×dt×20)` 做分母** |
| **炮塔目标附近震荡** | **06-10** | **抽象齿轮模式：RPM 阶跃（0↔±80 瞬变）+ 角度累积（开环）+ PD 伺服（闭环）= 双重积分器震荡** | **废弃抽象齿轮模式，改用位置模式 PD 伺服（参考 SwivelBearing）+ AimController 绝对角度定位** |
| **Create RPM 阶跃导致震荡** | **06-10** | **DebugGear 实测证明 Create RPM 变化是瞬时的（0→±16，±16→±88，±88→0 均在 1 tick 内），无渐变过程 → 抽象齿轮的"虚拟 RPM + 角度累积"开环系统无法收敛** | **位置模式 PD 伺服直接设定目标角度，不依赖 RPM 累积** |
| **setMotor 后续调用不响应** | **06-10** | **SmartBlockEntity 无 RPM 感知，setMotor 仅首次调用有效** | **TurretBaseBE 改为 KineticBlockEntity + SwivelBearing 模式 position-mode setMotor 每 tick** |
| **炮塔方向相反（atan2 符号镜像）** | **06-10** | **atan2(cross,dot) 以逆时针为正，约束 setMotor 以顺时针为正** | **`targetYawRad = -(currentYawRad + yawErr)` 取反** |
| **速度伺服 + SNAP teleport 到位不稳** | **06-10** | **恒速旋转到死区后用 teleport 跳转，solver substep 过冲导致左右晃动** | **位置模式 PD 伺服 + 绝对角度定位，无跳转** |
| **forward 方向 Bug** | **06-10** | **原代码用 (0,1,0)=UP 向量作为炮塔前方 → identity 姿态下投影 XZ=0 永远提前 return** | **改为 (0,0,1) Z 轴正向** |
| **车体被撕裂** | **06-10** | **SubLevel 质量 1.0 + 扭矩过大** | **SubLevel 质量降至 0.001，反作用力 ≈ 0** |
| **弹道渲染每帧扫描 SubLevel 导致卡顿** | **06-11** | BulletTrailRenderer 每帧遍历 Chunk 找炮塔 | 开火时缓存炮口数据，渲染器直接读 |
| **鼠标左键开火不响应** | **06-11** | InputEvent.MouseButton 是抽象类，NeoForge 总线无法直接监听 | 移除独立处理器，改在 onClientTick 中用 isDown() 检测 |
| **冷却计数器不在线** | **06-11** | fireCooldown 只在点击时递减，长时间不操作后卡住 | 改用 mc.level.getGameTime() 计时 |
| **高低机重力下垂射弹偏低** | **06-11** | 悬臂结构 PD 位置模式需稳态误差对抗重力 | PITCH_SERVO_STIFFNESS 500→6000，DAMPING 80→400 |
| **弹道方向与炮管不匹配** | **06-11** | 弹道使用摄像机位置算方向 | 改为从避雷针位姿计算炮管朝向 (0,0,1) 旋转到世界空间 |
| **服务端命中位置偏移（瞄准方块底部）** | **06-11** | 服务端独立做向下射线检测 | 直接使用客户端发送的精确命中坐标 |
| **WeaponOverlay 方法返回类型错误** | **06-11** | computeBarrelTipAndFwd() 改为 void 后留有 return null | 移除 return null 语句 |
| **弹道特效终点在实体脚部** | **06-11** | entityHit.getLocation() 在某些路径下返回 entity.position()（脚部位置） | 改用 AABB.clip() 手动计算精确交点 |
| **瞄准点方块底部/边缘偏移** | **06-11** | getBlockFaceCenter() 几何中心与实际瞄准方向有偏差 | 改用 blockHit.getLocation() 精确表面点 |
| **花草树叶阻挡瞄准/伤害射线** | **06-11** | ClipContext.OUTLINE 检测所有方块，花草树叶不应挡弹道 | 改用 COLLIDER + 树叶穿透循环 || **配置菜单显示键名而非中文** | **06-11** | 第一层使用错误的 config.iac_p.* 键前缀 | **改为 iac_p.configuration.*，补齐三级菜单所有纯键名/button/tooltip** |
## ⚠️ 当前活跃问题

| 问题 | 优先级 | 类别 | 说明 |
|------|--------|------|------|
| 重量仅上车时同步一次 | 低 | 限制 | 断线重连无二次同步 |
| 客户端无 `MassData` API | 低 | 限制 | 质量必须从服务端同步 |
| `physicsTick` 线程安全 | 低 | 隐患 | engineRpm 双端读写无同步（未观察到异常） |
| 多人摄像机同步 | 低 | 待验证 | Plan B1 尚未多人实测 |
| 编译依赖提取 | 中 | 流程 | 首次编译需手动提取嵌套 JAR |
| 锚点更新后约束未跟随 | 低 | RotaryConstraint.frame | 用户修改锚点坐标后，约束的 frame 位置未更新，可复用 GenericConstraintHandle.setFrame1/2 |
| `ModCreativeModeTabs` 命名 | 低 | 装饰 | 当前为 `"bettercookie_tab"` |
| F3+B 碰撞箱可见 | 低 | 装饰 | 需 mixin `renderHitbox` |
| 原版纹理待替换 | 低 | 装饰 | 驾驶舱使用原版纹理 |
| **SPRING_STIFFNESS_PER_NM=400（重车封顶 2000 N/m）偏软** | **中** | **调参** | **弹簧刚度基数 400 N/m per nm，重载时悬挂可能支撑不足** |
| **P 控制器增益 0.2 配合上限产生憋熄火感** | **低** | **设计特征** | **重车扭矩上限截断 → 负载因子飙升 → 转速暴跌，可能过于剧烈** |
| **轮胎款式的物理属性未区分** | **中** | **设计缺口** | **当前4种轮胎仅半径不同，其余属性（胎宽/刚度/滚阻/摩擦系数等）使用统一默认值，未来需通过 TireType 注册表扩展** |
| **偏航校准偏移需实测调校** | **🟡** | **调参** | **Config.java 已添加 TURRET_YAW_OFFSET（范围 -180~180°，默认 0°），需在游戏中瞄准目标测量偏移后填入正确值** |
| **瞄空气时炮塔持续指向无限远目标** | **低** | **特征** | **射线无命中时返回 1000 格末端点，炮塔持续指向鼠标方向，松开鼠标后保留最后位置** |

## 📋 待办功能
> 战略方向：**Blocky car 路线**。放弃等待 Sable 缩放，以 Minecraft 格子为基准，先出可玩成品。
> 资产策略：**不做官方的音效/特效/纹理包，做素材包导入框架，交给社区**（详见 `5-技术参考/5.4-设计哲学与路线图.md §8`）

| 功能 | 优先级 | 说明 |
|------|--------|------|
| WASD 智能映射 | ⭐⭐⭐ | 多轮协调输入抽象层 |
| 部件损坏系统 (Part HP) | ⭐⭐⭐ | Sable 自动分裂 + 方块级耐久 |
| 素材包导入系统 (ResourcePackImport) | ⭐⭐⭐ | 社区自制武器纹理/开火特效/轮胎悬挂/音效的挂载框架 |
| 素材包管理器 GUI (ResourcePackManager) | ⭐⭐ | 游戏内浏览/安装/切换/冲突检测素材包 |
| 轮胎滑转粒子 | ⭐⭐ | 摩擦圆饱和时烟雾/胎痕 |
| 参数配置化 | ⭐⭐ | 常量 → 配置文件（炮塔锚点已部分实现 Config.java 节，但代码改用硬编码；后续可切换回 Config 引用） |
| 智能变速 | ⭐⭐ | 自动调整传动比 |
| 多驾驶舱类型 | ⭐⭐ | 扩展驾驶舱组 |
| 载具状态 HUD | ⭐⭐ | 美观 HUD 替代调试覆盖层 |
| 素材包开发者 API 文档 | ⭐⭐ | 开放素材覆盖接口，支持第三方自定义特效 |
| 驾驶舱破坏保护 | ⭐ | 车内时不可破坏 |
| **扭矩/重量比调优** | **⭐** | **TORQUE_WEIGHT_RATIO=0.02 是否能让 10kg 小车正常跑** |
| **TireType 注册表（不同轮胎款式各有物理属性）** | **⭐⭐** | **当前4种轮胎仅半径不同，未来需支持越野胎/赛车胎/重载胎等款式，内置完整物理规格** |
| **偏航校准偏移游戏内微调** | **⭐** | **当前 Config 值需重启游戏，未来可添加游戏内 GUI 实时调整** |

## 🧊 已暂停 / 搁置

| 项目 | 暂停于 | 原因 | 恢复条件 |
|------|--------|------|---------|
| SubLevel 物理缩放（比例耦合） | 06-05 | Sable 上游 Rust 侧缩放功能烂尾，补全后实际效果无变化；进一步深入属于"用胶水修饼干"式的不划算投入 | 上游 ryanhcode/sable 正式实现 Pose3d.scale → Rapier 物理引擎的原生支持；或我们找到更轻量的替代方案 |

详见 `7-历史记录/7.1-缩放考古记.md`。

## ✅ 已验证的代码行为

- 上车后 `RenderPlayerEvent.Pre.setCanceled(true)` + `setInvisible(true)` — 玩家完全不可见
- `noPhysics=true` + 服务端零碰撞箱 — 免疫伤害和碰撞
- `setPos()` 替代 `teleportTo()` 不触发位置包 — 无 lerpSteps 拉回
- `SUBLEVEL_OCCUPANTS` 断线/销毁/下车均正确清理 — 代码走查
- 摩擦预算不能含阻尼（回弹行程塌陷）— 实测验证
- targetSpeed 与 getVelocity 统一 m/s — 代码走查
- RR/SSD 使用 nm 而非 sm（支持 30t 战车）— 代码走查
- 下车 UUID 查找不依赖玩家位置 — 代码走查
- CockpitBlock 实现 IBE 提供 ticker — 代码走查
- 降级兼容：无驾驶舱回退固定 RPM/Torque — 代码走查
- 油门直接扫描无时序竞争 — 代码走查
- 5 档直接档轮端 RPM = 发动机 RPM — 代码走查
- **载荷转移使载具"活过来"** — 用户实测通过 ✅
- **Brush 模型替代纯阻尼** — 编译通过，用户测试中 ✅
- **侧偏角钳制 ±45° + totalSpeed > 1.0 防抖** — 编译通过 ✅
- **质量自适应扭矩（effectiveTorque = mass × g × 0.02）** — 编译通过 ✅
- **连续油门（throttleLevel 0.0~1.0）** — 编译通过 ✅
- **扭矩平衡负载模型（torqueRatio = 1 - loadFactor）** — 编译通过 ✅
- **摩擦需求比显示用自然预算（非 500×膨胀）** — 代码走查 ✅
- **轮胎参数系统精简：6个调试残留运行时参数移除，仅保留胎压** — 编译通过 ✅
- **TireConfigC2SPacket 从 7 参数简化为仅 (BlockPos, double nominalPressure)** — 编译通过 ✅
- **VehicleKeyConfigScreen 从 7 输入框简化为单一胎压输入** — 编译通过 ✅
- **胎压影响滚动阻力/爆胎风险/抓地力** — 代码走查 ✅
- **GenericConstraint 优于 FixedConstraint（多SubLevel约束链稳定性）** — 实测通过 ✅
- **同位置同姿态叠加 SubLevel 可行（setContactsEnabled(false)）** — 实测通过 ✅
- **约束链无需跨级加额外约束** — 实测通过 ✅
- **12个ANCHOR_*硬编码常量正确替代Config引用** — 编译通过 ✅
- **射线检测排斥自身载具 SubLevel AABB** — 代码走查 ✅
- **B 键单发 + 每 tick 持续自动瞄准（已取消 5 tick 冷却）** — 代码走查 ✅
- **addLinearAndAngularVelocity 使用世界空间坐标** — Rust 源码确认：`rb.set_angvel(rb.angvel() + vector, wake_up)` ✅
- **getAngularVelocity 返回全局（世界空间）角速度** — Javadoc 确认 ✅
- **PlayerMountTracker.tick() 每 server tick 调用 TurretAimController.tick()** — 代码走查 ✅
- **TurretAimController TARGETS 遍历使用显式 Iterator（非 removeIf）** — 代码走查 ✅
- **位置模式 PD 伺服（SwivelBearing 模式）每 tick position-mode setMotor** — 编译通过 ✅
- **absolute 角度定位替代增量调节** — 编译通过 ✅
- **UpdateYawServo() 使用 angleLerp partialTick 插值（参考 SwivelBearing）** — 代码走查 ✅
- **atan2(cross,dot) 约束符号镜像：targetYawRad = -(currentYawRad + yawErr)** — 编译通过 ✅
- **Create RPM 阶跃特性（DebugGear 实测）：0→±16，±16→±88，±88→0 均在 1 tick 内完成** — 实测通过 ✅
- **setMotor 在 KineticBlockEntity 下每 tick 正常响应（位置模式）** — 实测通过 ✅
- **setMotor velocity mode（isVelocity=true）在 RotaryConstraint 上无效** — 实测通过 ❌
- **车体安全（SubLevel 质量 0.001 防反作用）** — 实测通过 ✅
- **炮塔稳定追踪目标，无震荡** — 用户实测通过 ✅
- **瞄空气（射线无命中时返回末端点，类型 "Air"，距离 1000m）** — 代码走查 ✅
- **WeaponOverlay 距离从 500 格扩展到 1000 格** — 代码走查 ✅
- **Config TURRET_YAW_OFFSET 可配置偏航校准偏移（范围 -180~180°，默认 0°）** — 编译通过 ✅
- **DebugGearBlock/DebugGearBlockEntity（N 键切换，每 tick 打印 RPM）** — 编译通过 ✅
- **车体局部坐标系稳定瞄准（vehicleSubLevelId + qVehInv 变换）** — 编译通过 ✅
- **高低机位置模式 PD 伺服（kP=6000/kD=400，position-mode setMotor）** — 编译通过 ✅
- **多炮台独立开火（fireAllTurrets 遍历 + 独立发包）** — 编译通过 ✅
- **炮口射线与摄像机瞄准分离** — 编译通过 ✅
- **方块面中心瞄准→精确命中点（blockHit.getLocation()）** — 编译通过 ✅
- **实体命中 AABB.clip() 精确交点** — 编译通过 ✅
- **穿透名单系统（COLLIDER + 树叶穿透循环 + isPenetrable）** — 编译通过 ✅
- **按住连发（isDown + 3 tick cooldown）** — 编译通过 ✅
- **无视无敌帧（invulnerableTime=0）** — 编译通过 ✅
- **调试轴线默认隐藏，F3+B 时显示** — 编译通过 ✅
- **弹道渲染零 SubLevel 扫描（仅读缓存）** — 代码走查 ✅
- **客户端本地弹道 + 服务端仅伤害（无服务端弹道验证）** — 代码走查 ✅
- **VehicleKeyConfigScreen 所有标签改为 Component.translatable()** — 编译通过 ✅
- **GrindstoneConfigScreen 所有标签改为 Component.translatable()** — 编译通过 ✅
- **VehicleDebugOverlay 8 标签改为 Component.translatable("debug.iac_p.overlay.*")** — 编译通过 ✅
- **ClientEvents 开火/射线/调试消息改为 Component.translatable("message.iac_p.*")** — 编译通过 ✅
- **TurretTargetC2SPacket 服务端消息改为 Component.translatable()** — 编译通过 ✅
- **Config.java 所有 .comment() 英中双语** — 代码走查 ✅
- **en_us.json 约 100 条目完整覆盖** — 编译通过 ✅
- **zh_cn.json 约 100 条目完整覆盖** — 编译通过 ✅
- **配置翻译键 iac_p.configuration.* 三级菜单全层级可读** — 游戏实测通过 ✅
- **config.iac_p.* 应为 iac_p.configuration.*（TranslationChecker 日志验证）** — 实测通过 ✅
