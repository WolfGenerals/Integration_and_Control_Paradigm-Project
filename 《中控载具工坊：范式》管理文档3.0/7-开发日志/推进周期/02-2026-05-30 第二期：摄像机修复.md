# 2026-05-30 第二期：摄像机修复

### 完成
- Plan B 摄像机（消除抖动）
- Plan B1 轨道摄像机（最终方案）：`CameraMixin` + `renderPose(partialTick)` 球面环绕

### 踩坑
- `teleportTo()` 触发 `ClientboundPlayerPositionPacket` → lerpSteps 插值抖动
- 修复：改用 `setPos()` 配合每 tick 持续刷新
