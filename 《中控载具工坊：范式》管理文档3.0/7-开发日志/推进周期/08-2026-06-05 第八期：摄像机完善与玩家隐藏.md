# 2026-06-05 第八期：摄像机完善 + 玩家隐藏

### 完成
- **自适应摄像机系统**：高度 = `bbox.maxY + 1 + 手动偏移`，距离 = `手动距离 + 最长边/2`
- **摄像机极点奇点修复**：俯仰 ±90° 时 `horizontalDist=0` → `atan2(0,0)` 偏航突变，改为沿用 entity.getYRot()
- **玩家完全隐藏**：`RenderPlayerEvent.Pre.setCanceled(true)` + `setInvisible(true)`
- **PlayerModelRotationMixin 废弃删除**：旋转对齐方案因 yaw 与 yBodyRot 双重冲突无法解决，放弃

### 踩坑

| 问题 | 根因 | 修复 |
|------|------|------|
| 玩家模型随摄像机旋转 | SubLevel 全四元数与 yBodyRot 双重叠加 | 完全隐藏模型 |
| 摄像机极点偏航角突变 | 俯仰 ±90° 时 `horizontalDist=0` | 沿用 `entity.getYRot()` |
| 玩家贴身渲染导致黑屏 | 零碰撞箱时玩家位置在 (0,0,0) | 零碰撞箱设于当前位置 |
| 行走动画渲染阶段覆盖 | `LivingEntity.updateWalkAnimation()` | 直接取消渲染事件 |
