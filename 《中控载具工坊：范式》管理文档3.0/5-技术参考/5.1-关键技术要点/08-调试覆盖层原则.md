# 八、调试覆盖层原则

**始终用常量引用，不硬编码数值**：

```java
// ❌ 错误：硬编码
displayLines.add(line("发动机", "3,200 RPM | 30 Nm"));

// ✅ 正确：引用编译时常量
displayLines.add(line("发动机",
    String.format("%,.0f RPM | %.0f Nm", engineRpm, PowertrainConstants.ENGINE_TORQUE)));
```
