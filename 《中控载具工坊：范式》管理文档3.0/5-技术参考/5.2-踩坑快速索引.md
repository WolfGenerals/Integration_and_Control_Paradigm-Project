# 踩坑快速索引

> 06-14 更新：新增下车坐标系 5.1-32 详细文档；新增实验性直传下车模式 Config.java §DISMOUNT_EXPERIMENTAL_DIRECT；CAMERA_AIM 全域 PENETRATE_AABB + WeaponOverlay 射线重建
>
> 按症状查找已知问题、根因、解决方案。
> 此处仅作索引，详细信息参见对应文档。

---

## 按症状索引

| 症状 | 根因 | 快速修复 | 详见 |
|------|------|---------|------|
| **刹车 18g 钉地** | `brakeMag` 用了含 51 倍膨胀的 `frictionBasis` | 用 `springImpulse`（真实法向冲量） | `2-功能清单/2.3` §手刹 |
| **松开 W 后车还在加速** | `putIfAbsent(false)` 不覆盖 + `enqueueWork` 晚于 BE tick | 废弃共享 Map，直接扫描 SubLevel | `4-进度与规划/4.1-当前状态.md` §06-04 |
| **CockpitBE.tick() 不执行** | `EntityBlock` 不提供 ticker | 改用 `IBE<CockpitBlockEntity>` | `5-技术参考/5.1` §一 |
| **RPM 在调试覆盖层不更新** | 同上一 tick() 不执行 | 改用 IBE 接口 | `4-进度与规划/4.1-当前状态.md` §06-04 |
| **极速超音速（不合理）** | 扭矩 80 Nm 过高 + 5 档超比档 0.7 | 扭矩 80→30，齿比 0.7→1.0 | `4-进度与规划/4.1-当前状态.md` §06-04 |
| **质量显示不准确** | `getInverseNormalMass()` ≠ 总质量 | 改用 `MassData.getMass()` | `5-技术参考/5.1` §七 |
| **SubLevel 内永远扫不到方块** | `logicalPose().transformPosition()` 用于方块访问 | 用 `getLoadedChunks()` + chunk 偏移 | `3-架构与文件结构/3.1` §坐标 |
| **上车后画面抖动** | `teleportTo()` 触发位置同步包 | 改用 `setPos()` 每 tick 刷新 | `5-技术参考/5.1` §九 |
| **编译报 `SubLevelAccess` 找不到** | companion JAR 未提取到 classpath | 从 sable JAR 提取 sable-companion | `1-项目介绍/1.2` §编译 |
| **编译报 flywheel/ponder 找不到** | JarJar 嵌套 JAR 未手动提取 | 运行 Python 提取脚本 | `1-项目介绍/1.2` §编译 |
| **驾驶舱放置后上格不生成** | `onPlace()` 中 `!oldState.is(this)` 判断 | 检查方块状态新旧比较 | `2-功能清单/2.2` §驾驶舱 |
| **上/下车后状态未恢复** | 断线重连 NBT 残留 | `EntityJoinLevelEvent` 清理 | `2-功能清单/2.2` §上车 |
| **摩擦圆饱和时驱动力消失** | 摩擦预算含阻尼分量 | 只用 `springImpulse` 作为基数 | `5-技术参考/5.1` §四 |
| **车开不动（速度极低）** | targetSpeed 单位差 20 倍 | 物理 tick 中去掉 `/20.0` | `5-技术参考/5.1` §四 |
| **重车操控性异常（像开船）** | 滚动阻力/侧滑阻尼用 `sm`（上限 200） | 改用 `nm` 随质量缩放 | `5-技术参考/5.1` §四 |
| **StreamCodec 编译报错** | `composite()` 超过 8 字段 | 手动编解码 | `5-技术参考/5.1` §十 |
| **SubLevel 缩放无效** | Sable Rust 侧 `Pose3d.scale` 从未实现 | 搁置，等待上游 | `7-开发日志/7.1-缩放考古记.md` |
| **Rust 编译 f32/f64 类型不匹配** | `collision_boxes` 是 `Vec<(f32,...)>` 但 scale 是 `Vector3<f64>` | 显式 `as f64` 转换 | `7-开发日志/7.1-缩放考古记.md` |
| **魔改前置 JAR 污染 libs 目录** | CI 构建的 artifact 直接覆盖了原版 JAR | `git checkout` 复原；务必实验后检查 `git status -- libs/` | `7-开发日志/7.1-缩放考古记.md §收尾清理` |
| **调试信息显示值与实际不符** | 调试覆盖层硬编码了字符串 | 改为引用编译时常量 | `5-技术参考/5.1` §八 |
| **摄像机拉到最高/最低时画面水平旋转** | 俯仰 ±90° 时 `horizontalDist=0` → `atan2(0,0)=0` → 偏航突变为 -90° | `horizontalDist < 1e-4` 时沿用 `entity.getYRot()` | `5-技术参考/5.1` §十一 |
| **玩家模型随摄像机旋转** | SubLevel 全四元数（含 yaw）与 yBodyRot 双重叠加 | 放弃旋转对齐，完全隐藏玩家模型 | `4-进度与规划/4.1-当前状态.md` §06-05 |
| **上车后玩家模型可见但旋转不对** | 模型旋转与摄像机 yaw 冲突 | 直接取消 RenderPlayerEvent 渲染 | `5-技术参考/5.1` §十三 |
| **玩家模型渲染导致黑屏/透视剔除** | 零碰撞箱时玩家位置在世界原点 → 视锥体裁剪 | 零碰撞箱设于玩家当前位置 | `4-进度与规划/4.1-当前状态.md` §06-05 |
| **转向时车减速抖动** | Brush 模型中 `tan(α)` 在大侧偏角发散 → 侧向力震荡 | 侧偏角钳制 ±45° + totalSpeed > 1.0 阈值 + 平滑混合 | `5-技术参考/5.1` §十八 |
| **原地停放自旋** | `forwardSpeedAbs > 0.5` 阈值过低，微小扰动触发 Brush | 改为 `totalSpeed > 1.0`，低速纯阻尼 | `5-技术参考/5.1` §十八 |
| **摩擦需求比永远偏低（~17%）** | 显示分母用了 500× 膨胀的 frictionBudget | 改用自然预算：`max(adjustedSpringImpulse, nm×dt×20)` | `5-技术参考/5.1` §十九 |
| **载荷转移时侧向感觉不对** | 侧向转移公式符号可能需要反转 | 验证右转时右轮是否应减载 | `5-技术参考/5.1` §十七 |
| **轻车（~10kg）开不动** | TORQUE_WEIGHT_RATIO 使扭矩过小 | 调整 TORQUE_WEIGHT_RATIO 或 P 控制器增益 | `2-功能清单/2.4` §1.2 |
| **重车起步憋熄火** | P 控制器力被 maxWheelForce 截断 → loadFactor 爆表 | 增大 maxWheelForce 或降低 RPM_LOAD_DECEL | `2-功能清单/2.4` §1.4 |
| **连续油门无自动衰减导致"鬼车"** | 松开 W 后 throttleLevel 不降，车继续加速 | **06-08 已修复**：添加 THROTTLE_DECAY=0.015（自动滑行衰减）和 THROTTLE_BRAKE_RATE=0.04（S 键主动减油） | `2-功能清单/2.4` §1.3 |
| **SPRING_STIFFNESS_PER_NM=400 偏软** | 弹簧刚度基数 400 N/m per nm，重载可能支撑不足 | 测试后决定调高 STIFFNESS_PER_NM 或降低 MASS_THRESHOLD | `2-功能清单/2.3` §参数速查 |
| **轮胎参数太多，玩家无所适从** | 7 个轮胎物理参数全部暴露为运行时 NBT 可编辑字段，属于早期调试残留 | 06-09 精简：仅保留胎压，其余改为编译时常量；轮胎款式决定物理属性 | `2-功能清单/2.3` §7 |
| **NBT 字段膨胀导致维护负担** | 每个运行时字段需要 TAG 常量 + read/write + 网络同步 + 兼容性检查 | 能用编译时常量的就不要做成运行时字段；谨慎评估每个字段是否真的需要玩家在游戏内调节 | `2-功能清单/2.3` §7 |
| **RenderLevelStageEvent 事件不触发** | `@EventBusSubscriber` 注册到模组总线而非游戏总线 | 在 `IACPClient` 中用 `NeoForge.EVENT_BUS.register(ClassName.class)` 手动注册 | `5-技术参考/5.1` §二十 |
| **自定义世界线渲染不可见** | Tesselator/BufferUploader 或 RenderType.LINES 均未显示 | 改用 Sable debug render 模式：PoseStack × modelViewMatrix + 手动 camera 偏移 + MultiBufferSource + RenderType.LINES + endLastBatch | `5-技术参考/5.1` §二十一 |
| **RotaryConstraint 导致载具疯狂旋转** | pos1/pos2 传了世界坐标 | 使用底层绝对方块坐标：`getCenterBlock()+(0.5,0.5,0.5)` | `5-技术参考/5.1` §二十四 |
| **RotaryConstraint 砂轮始终无法连接** | pos1/pos2 用了嵌入层相对坐标 `(0.5,0.5,0.5)` | 改为底层实际坐标：`getCenterBlock()+0.5` | `5-技术参考/5.1` §二十四 |
| **约束创建后 SubLevel 消失** | 约束位置在 SubLevel 找不到块 → 物理引擎移除 | 确认 pos1/pos2 在该 SubLevel 的 plot 范围内 | `5-技术参考/5.1` §二十四 |
| **炮塔方向镜像（90° 指向 270°）** | atan2(cross,dot) 逆时针为正，约束 setMotor 顺时针为正 | 取反：`-(currentYawRad + yawErr)` | `5-技术参考/5.1` §二十九 |
| **炮塔在目标附近震荡** | 抽象齿轮模式中 RPM 阶跃导致双重积分器震荡 | 改用位置模式 PD 伺服 | `5-技术参考/5.1` §二十九 |
| **setMotor velocity mode 不旋转** | RotaryConstraint 不支持 velocity mode | 使用 position mode：`setMotor(axis, goal, kP, kD, false, 0)` | `5-技术参考/5.1` §二十九 |
| **Create RPM 行为与预期不符** | Create 应力网络 RPM 变化是阶跃，无渐变 | 不依赖 RPM 做平滑控制，用 position-mode setMotor | `5-技术参考/5.1` §二十八 |
| **射线检测永远返回载具自身** | 摄像机在载具 AABB 内部，rayAABBIntersection 返回表面 | 跳过当前乘坐 SubLevel：`mountedUUID.equals()` | `2-功能清单/2.2` §炮塔底座 |
| **炮塔不持续追踪（松手后停止）** | 旧代码 raycastCooldown 导致 5 tick 才更新一次 | 取消冷却，每 client tick 执行 | `ClientEvents.java` |
| **WeaponOverlay 瞄空气也有效果** | 无命中时返回射线末端点（类型 "Air"） | 炮塔持续指向鼠标方向 | `WeaponOverlay.java` |
| **StreamCodec 超 8 字段编译报错** | `composite()` 参数数量限制 | 手动编解码 `new StreamCodec<>() { ... }` | `5-技术参考/5.1` §十 |
| **瞄准自己炮塔时炮塔自瞄转圈** | CAMERA_AIM 射线穿过炮塔 SubLevel AABB 被报告为命中 | 移除 raycastGeneric() 中 SubLevel AABB 检测段；改用局部空间 clip + 坐标变换 | `5-技术参考/5.1` §三十三 / `WeaponOverlay.java` |
| **SubLevel 内方块命中坐标偏移（瞄不到正确位置）** | mc.level.clip() 返回 SubLevel 局部 plot 坐标，非主世界坐标 | 变换射线到 SubLevel 局部空间后 clip，再用 logicalPose.transformPosition() 变换回主世界 | `5-技术参考/5.1` §三十三 / `WeaponOverlay.java §raycastGeneric` |
| **S2C 包注册用 playToClient** | 错用 playToServer 导致包不被接收 | 服务端→客户端用 `registrar.playToClient()` | `2-功能清单/2.5` §网络 |
| **反转方向后车辆不响应（油门不上升）** | 按键层 W↔S 交换后 `throttleBackward=true`，但引擎层硬编码 `direction<0` = 松油门 | CockpitBE 新增 `smartMappingReversed` 标志，反转方向解读 + 齿比符号反转 | `CockpitBlockEntity.java`、`6. WASD智能映射.md` |
| **反转方向后需手动开关智能映射才生效** | 客户端发送 REVERSE 后本地 BE 智能键未同步，仍用旧键读取键盘 | `ClientMountHandler.localSwapSmartKeys()` 在发送包后立即交换客户端本地智能键 | `ClientMountHandler.java`、`6. WASD智能映射.md` |
| **下车传送到车底（手刹下车）** | `findGroundDismountPosition()` 使用 `logicalPose().position()` 物理空间 XZ + plot chunk 固定世界坐标 Y，collisionBlocks 读的是 plot 原始位置不反映当前物理刚体位置 | 从玩家当前位置（上车时每 tick 同步的世界坐标）出发搜索，或通过 `logicalPose` 将 SubLevel 局部坐标变换到世界空间 | `ServerMountHandler.java` §findGroundDismountPosition |
| **F 下车传送到百米高空** | `findVehicleTopDismountPosition()` 中 `baseX/baseZ` 来自 `logicalPose().position()`（物理空间），`topY` 来自 plot chunk 世界坐标，车辆运动后两者偏移 → (XZ mismatch, Y 指向远处高空) | 统一坐标系：全部从玩家当前位置出发，或全部通过 `logicalPose.transformPosition()` 将局部坐标变换到世界空间 | `ServerMountHandler.java` §findVehicleTopDismountPosition |
| **Lambda 中修改变量编译报错** | Java lambda 要求捕获的局部变量必须是 effectively final | 改用单元素数组（如 `int[] count={0}`），或使用 `AtomicInteger`/`AtomicReference`；实例字段可以直接修改 | `SubLevelScanner.java`，所有使用方 |
| **collectSubLevelBlockPositions 不反映物理碰撞体** | `SubLevelScanner` 遍历 `plot.getLoadedChunks()` 的固定世界坐标，Sable 物理刚体运动后两者不同步 | 这是已知限制，目前无法从 Java 侧直接查询 Sable 物理碰撞体的 AABB。下车逻辑中应将此作为松散约束而非精确碰撞检测 | `ServerMountHandler.java` §collectSubLevelBlockPositions |
| **武器命中 SubLevel 方块无效果** | `Sable.HELPER.getContaining(Vector3dc)` 以精确 3D 坐标查询，武器命中点在 AABB 表面不在空间索引内 | 改用 `SubLevelContainer` 遍历 + AABB 碰撞 + 最近方块搜索；或尝试 `SableCompanion.INSTANCE.getContaining(Level, BlockPos)` 方块整数坐标查询 | `5-技术参考/5.1-关键技术要点/30-SableAPI查询方式：Vector3dc vs SubLevel遍历.md` |
| **AABB 表面交点映射为空气（部件损坏无效果根因）** | `pose.transformPositionInverse(hitPos)` 将 AABB 表面点变换到局部空间后落在方块外部 | 采用 SableBridge 方案：将完整射线变换到 SubLevel 局部空间后重新 clip | `5-技术参考/5.1-关键技术要点/31-SubLevel射线追踪与归属管理.md` |
| **炮塔自伤（砂轮/避雷针被武器摧毁）** | `rayTraceSubLevels()` 遍历所有 SubLevel 时不排除自身衍生结构 | `SubLevelOwnership` 归属管理系统 + `rayTraceSubLevels` 排除集合参数 + 射线起点 BB 含含检查 | `5-技术参考/5.1-关键技术要点/31-SubLevel射线追踪与归属管理.md` |
| **断线重连后炮塔解体** | 世界重载后约束句柄丢失、`SubLevelOwnership` 归属记录丢失 | `TurretBaseBlockEntity.read()` 设置延迟重建，`onLoad()` 或 `tick()` 延迟执行（三段式：read→onLoad→tick 持续重试） | `TurretBaseBlockEntity.java` §deferredRebuildTicks |
| **炮塔重连后约束重建偶发失败** | `read()` 中 `reestablishConstraints()` 调用时 SubLevel 容器未就绪 | 改用 `onLoad()` 回调 + `tick()` 延迟重试机制，启用 `deferredRebuildTicks` 字段控制时序 | `TurretBaseBlockEntity.java` §deferredRebuildTicks、§onLoad() |
| **炮塔底座放置后需要右键才能召唤** | 旧行为需额外交互 | 新增 `TurretBaseBlock.setPlacedBy()` 放置时自动调用 `be.assemble()` | `TurretBaseBlock.java` §setPlacedBy |

---

## 按组件索引

### CockpitBlock / CockpitBlockEntity
| 问题 | 快速链接 |
|------|---------|
| tick() 不执行 → IBE 接口 | `5-技术参考/5.1` §一 |
| 放置后上格不生成 → onPlace 逻辑 | `2-功能清单/2.2` §驾驶舱 |
| 油门不响应 → 共享 Map 废弃 | `4-进度与规划/4.1-当前状态.md` §06-04 |

### SuspensionTestBlock / SuspensionTestBlockEntity
| 问题 | 快速链接 |
|------|---------|
| 刹车 18g → springImpulse | `5-技术参考/5.1` §五 |
| 车开不动 → 单位匹配 | `5-技术参考/5.1` §四 |
| 摩擦圆失效 → 预算含阻尼 | `5-技术参考/5.1` §四 |
| 重车异常 → sm vs nm | `5-技术参考/5.1` §四 |

### 网络包
| 问题 | 快速链接 |
|------|---------|
| StreamCodec 8 字段限制 | `5-技术参考/5.1` §十 |
| 注册方式 → Payload 事件 | `2-功能清单/2.5` §网络 |

### 载荷转移系统
| 问题 | 快速链接 |
|------|---------|
| 侧向符号可能反转 → 物理验证 | `5-技术参考/5.1` §十七 |
| COG_HEIGHT/HALF_WHEELBASE 估算不准 | `2-功能清单/2.3` §5 |

### TurretBase / 炮塔约束链
| 问题 | 快速链接 |
|------|---------|
| 约束坐标用世界坐标 → 载具撕碎 | `5-技术参考/5.1` §二十四 |
| 约束坐标用嵌入层相对坐标 → 砂轮滑走 | `5-技术参考/5.1` §二十四 |
| 约束位置导致 SubLevel 消失 | `5-技术参考/5.1` §二十四 |
| FixedConstraint 撕裂载具 | `5-技术参考/5.1` §二十六.4 |
| 避雷针飞走/消失 → pose 不一致 | `5-技术参考/5.1` §二十六.2 |
| 跨级加约束导致干扰 | `5-技术参考/5.1` §二十六.3 |
| 偏移位置导致碰撞体重叠 | `5-技术参考/5.1` §二十六.1 |
| setMotor 后续调用不响应 → SmartBlockEntity 无 RPM 感知 | `4-进度与规划/4.1-当前状态.md` §06-10（改用 KineticBlockEntity） |
| addLinearAndAngularVelocity 无效果 → forward 方向 Bug：(0,1,0)=UP | `4-进度与规划/4.1-当前状态.md` §06-10 |
| 射线检测总返回载具自身 → 排除当前 SubLevel AABB | `2-功能清单/2.2` §炮塔底座 §自动瞄准 |
| TurretAimController 无日志输出 → tick/phys/gh 未就绪 | `4-进度与规划/4.1-当前状态.md` §06-10 |
| 炮塔方向镜像 → atan2 逆时针为正，约束顺时针为正 | `4-进度与规划/4.1-当前状态.md` §06-10（`targetYawRad = -(currentYawRad + yawErr)`） |
| 炮塔震荡（抽象齿轮模式）→ RPM 阶跃导致双重积分器震荡 | `4-进度与规划/4.1-当前状态.md` §06-10（改用位置模式 PD 伺服修复） |
| 速度伺服到位不稳 → solver substep 过冲 | `4-进度与规划/4.1-当前状态.md` §06-10（改用位置模式 PD 伺服） |
| setMotor velocity mode 无效 → RotaryConstraint 不支持 velocity mode | `5-技术参考/5.1` §二十九（必须用 position mode） |
| Create RPM 变化不可预测 → 实际为阶跃，无渐变 | `5-技术参考/5.1` §二十八 |

### Brush 轮胎模型
| 问题 | 快速链接 |
|------|---------|
| 转向抖动 → 侧偏角钳制 + 混合过渡 | `5-技术参考/5.1` §十八 |
| 自旋 → totalSpeed 阈值 | `5-技术参考/5.1` §十八 |
| 残余抓地 25%→10% → 减抖动 | `4-进度与规划/4.1-当前状态.md` §06-07 |

### 轮胎物理参数系统 (Tire Physics)
| 问题 | 快速链接 |
|------|---------|
| 参数过多 → 06-09 精简为仅胎压 | `2-功能清单/2.3` §7 |
| 调试残留运行时字段 → 改为编译时常量 | `2-功能清单/2.3` §7 |
| NBT 字段膨胀 → 谨慎评估必要性 | `2-功能清单/2.3` §7 ⚠踩坑教训 |
| 未来扩展 TireType 注册表 | `2-功能清单/2.3` §7 设计原则 |

### 质量自适应扭矩
| 问题 | 快速链接 |
|------|---------|
| 轻车扭矩过小 → 调 TORQUE_WEIGHT_RATIO | `2-功能清单/2.4` §1.2 |
| 服务端独占 API → 异常回退 | `5-技术参考/5.1` §十四 |

### 连续油门系统
| 问题 | 快速链接 |
|------|---------|
| 无自动衰减是特征非 Bug | `5-技术参考/5.1` §十五 |
| 扫描方式 → CockpitBE 直接扫描 | `2-功能清单/2.4` §架构 |

### 显示值分离
| 问题 | 快速链接 |
|------|---------|
| frictionDemandRatio 显示偏低 → 自然预算 | `5-技术参考/5.1` §十九 |

### TurretBase / 炮塔系统
| 问题 | 快速链接 |
|------|---------|
| SubLevel 内方块访问 → chunk 扫描 | `5-技术参考/5.1` §三 |
| UUID→方块实体反查 → 静态注册表 | `5-技术参考/5.1` §二十二 |
| 锚点数据推送 → S2C 包 vs NBT 同步 | `5-技术参考/5.1` §二十三 |
| 三色轴线不渲染 → ✅ Sable debug render 模式 | `5-技术参考/5.1` §二十一 |

### 客户端事件注册
| 问题 | 快速链接 |
|------|---------|
| @EventBusSubscriber 不触发游戏事件 | `5-技术参考/5.1` §二十 |
| RenderLevelStageEvent 收不到 | `5-技术参考/5.1` §二十 |
| 正确注册方式：NeoForge.EVENT_BUS.register() | `5-技术参考/5.1` §二十 |

### 网络包
| 问题 | 快速链接 |
|------|---------|
| StreamCodec 8 字段限制 → 手动编解码 | `5-技术参考/5.1` §十 |
| S2C 包注册用 playToClient | `5-技术参考/5.1` §二十三 |
| S2C 推送 vs NBT 同步选择 | `5-技术参考/5.1` §二十三 |

### SubLevelScanner
| 问题 | 快速链接 |
|------|---------|
| Lambda 变量捕获编译错误 → 用数组/Atomic 绕过 effectively final | 本页 §Lambda 中修改变量编译报错 |
| `forEachBlock` vs `forEachBlockState` 选择 | 仅需 BlockState 时用后者略高效 |

### 构建
| 问题 | 快速链接 |
|------|---------|
| companion JAR 缺失 | `1-项目介绍/1.2` §编译 |
| flywheel/ponder 缺失 | `1-项目介绍/1.2` §编译 |
