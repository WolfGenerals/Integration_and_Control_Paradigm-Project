# 当前状态

> 更新于 2026-06-15。仅维护活跃问题/待办/已验证行为。已完成功能见 `2-功能清单/2.1-功能总表.md`。

## ⚠️ 当前活跃问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| 重量仅上车时同步一次 | 低 | 断线重连无二次同步 |
| 客户端无 MassData API | 低 | 质量必须从服务端同步 |
| physicsTick 线程安全 | 低 | engineRpm 双端读写无同步（未观察到异常） |
| 多人摄像机同步 | 低 | 轨道摄像机尚未多人实测 |
| 锚点更新后约束未跟随 | 低 | RotaryConstraint.frame 位置未更新 |
| SPRING_STIFFNESS_PER_NM=400 偏软 | 中 | 重载时悬挂可能支撑不足 |
| 轮胎款式的物理属性未区分 | 中 | 4 种轮胎仅半径不同，未来需 TireType 注册表扩展 |
| AffiliationRegistry.onWorldLoad() 未接入事件总线 | 🔴 | 世界重载后注册表不会自动重置 |
| AffiliationRegistry.register() 非原子操作 | 🔴 | 多线程并发有竞态条件 |
| faction 阵营系统未实际启用 | 🔴 | 所有实体使用 FACTION_NEUTRAL |
| ComponentRegistry NBT 持久化未实现 | 🟡 | 有序列化方法但无 saveToNbt/loadFromNbt |
| 归属系统 NBT 缺少版本号 | 🟡 | 未来字段变更时旧存档静默失败 |

## 🧊 已暂停

| 项目 | 原因 | 恢复条件 |
|------|------|---------|
| SubLevel 物理缩放 | Sable 上游 Rust 侧缩放功能烂尾 | 上游实现 Pose3d.scale → Rapier 原生支持 |

## ✅ 已验证的代码行为（关键部分）

- `setPos()` 替代 `teleportTo()` 不触发位置包 — 无 lerpSteps 拉回
- `SUBLEVEL_OCCUPANTS` 断线/销毁/下车均正确清理
- 降级兼容：无驾驶舱回退固定 RPM/Torque
- 5 档直接档轮端 RPM = 发动机 RPM
- 位置模式 PD 伺服每 tick position-mode setMotor 正常响应
- Create RPM 阶跃特性：0→±16→±88→0 均在 1 tick 内完成
- setMotor velocity mode 在 RotaryConstraint 上无效（必须 position mode）
- AffiliationRegistry 注册/注销/组管理/玩家-载具绑定 全部可用
- ComponentRegistry 三层索引 / ComponentHost 自动注册注销 全部可用
- 瞄准系统全链路延迟优化：ComponentRegistry O(1) + driveImmediate 消除空等
